<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>t3 - Response Page</title>
</head>
<body>

    <nav class="navbar navbar-dark bg-dark mb-5">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Task 3 App</span>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                
                <div class="card card-custom animate-fade-in text-center p-5">
                    <div class="card-body">
                        <h1 class="display-4 text-success mb-3">Success!</h1>
                        <p class="lead">Welcome aboard, <strong><%= name %></strong>.</p>
                        <p class="text-muted">A confirmation email has been sent to <%= email %>.</p>
                        
                        <hr class="my-4">

                        <h4 class="mb-3">Current Members</h4>
                        <ul class="list-group list-group-flush text-start mx-auto" style="max-width: 400px;">
                            <% allUsers.forEach(function(user) { %>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <%= user.username %>
                                    <span class="badge bg-primary rounded-pill"><%= user.age %> y/o</span>
                                </li>
                            <% }); %>
                        </ul>

                        <div class="mt-5">
                            <a href="/" class="btn btn-outline-primary btn-transform">Add Another User</a>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

</body>
</html>